const express = require("express");
const router = express.Router();

router.get("/");
router.put("/");
router.post("/");
router.delete("/");

module.exports = router;

//UPDATE `product` SET `instock` = '25' WHERE `product`.`id` = 1;
